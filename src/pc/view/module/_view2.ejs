<li class="form__item">
    <dl>
        <dt>Select your specialist</dt>
        <dd>
            <div class="button-group">
                <input class="button-group__input input" placeholder="Seach doctors" ng-model="searchText" />
                <ul class="button-group__list">
                    <li ng-repeat="doctor in myCtrl.doctors | filter:searchText">
                        <button class="button" type="button" value="{{doctor.name}}" ng-click="myCtrl.selectDr($event)">{{doctor.name}}</button>
                    </li>
                </ul>
            </div>
        </dd>
    </dl>
</li>